# 配置、压缩与记忆整改进度

基线：develop@1df51729；保留实施前未提交修改及用户删除的文件。

批准方案：配置精简保持功能等价；自动压缩比例 0.9、压缩输出 131072、思考 max；全部应用提示词英文；禁止查询、证据、工具输出和标题截断；主动记忆 L2、感知 L1、按独立证据与强线索提炼 L2；L2 先搜索，相似更新，不存在才新增；消费为搜索、更新、删除三个工具。

## 实施阶段

- [ ] 配置与压缩：角色配置读取、真实 usage 触发、完整压缩输入、失败不覆盖历史。
- [ ] 英文提示词与已取消限制：资源统一、工具输出/标题/证据不截断。
- [ ] 记忆：L1/L2 生产与三个消费工具、出处计数、搜索与并发去重。
- [ ] 定向本地回归、三种日常环境真实验收。
- [ ] 恢复原三环境全量验收；全部门槛满足后才合并 master。

## 配置与范围记录

- 已批准配置只修复 JSON 尾逗号，并将三个主执行角色比例改为 0.9、compressor.max_tokens 改为 131072。
- 缺失的 model_gateway/execution 现有值列于 config-compaction-memory-config-review.md；等待字段确认后再补齐，期间不隐式从代码补值。
- 原工作区已有大量修改；按用户要求在现有 develop 与日常环境继续，不创建另一个工作区或替代 Python 环境。
- 旧测试中“输出预算导致提前压缩”“裁剪工具输出”等断言与批准方案冲突：保留基线文件哈希及失败证据，针对这些明确变更更新用例，其他断言保留。

## 验证记录

- 实施前 config.json 解析失败：第 95 行（上一字段尾逗号）。
- 配置及压缩第一组：`tests/test_config_compaction_contract.py` 11 项先失败，修改后 11 项通过（4.24 秒）。涵盖实际 usage 阈值、无输出预算提前压缩、内部参数不上 API、工具/引用正文完整；仅为本地辅助验证。
- 此文件只记录有实际证据的结果；真实模型验收与本地测试分别记录。

## 当前结果（2026-09-20，尚未完成验收）

### 配置差异

相对于本轮开始保存的源码配置，实际仅有以下变化：修复一个尾逗号；三个执行角色的压缩比例改为 0.9；压缩输出改为 131072；移除已无读取位置的 `memory_perception.input_context_ratio`、`existing_record_limit`。逐字段审计没有发现其他参数变化。

`model_gateway`、`execution` 的必要现有值仍待确认，尚未补进源码。五种结果的配置字段也尚待确认，代码中的旧 Outcome 枚举因此还未替换。不能宣称“配置独立完整”或“硬编码全部清除”。

已修复缺少 execution 时继承整个宿主环境、静默跳过权限检查的问题；现在执行前报告缺少的配置字段。执行策略类型错误会报告来源文件及字段，不填默认值或放宽权限。

### 本地辅助验证

| 证据文件（位于本轮 runtime 目录） | 实际结果 | 范围与限制 |
|---|---|---|
| targeted-final.txt | 68 通过 | 配置、压缩、记忆生产契约及全文引用；不是真实模型质量验收 |
| runner-prompt-config.txt | 65 通过 | 内循环、工具配对、压缩、英文回执、记忆工具及配置路径 |
| memory-final-local-v2.txt | 66 通过 | 真实 LanceDB 本地读写与索引恢复，模型部分为辅助夹具 |
| policy-types-green.txt | 46 通过、7 失败 | 六项依赖源码缺失的 model_gateway；另一个旧 context 口径用例已按批准的角色配置规则调整并单独复测通过 |
| full-local.txt / full-local-continued.txt | 未通过 | 两项测试仍引用工作区原本已删除／缺失的脚本；继续运行的批次 600 秒未结束，不能称全量通过 |

测试组之间存在重叠，不将通过数相加作为独立用例数量。

保留的修复前证据包括 `source-guards-red.txt`、`stale-usage-red.txt`、`manual-snapshot-red.txt`、`policy-types-red.txt`。整合时曾破坏独立索引阶段，已修正并增加“生产结果不重做、只重试索引”的回归；`index-stage-red.txt` 保留该失败。重试成功后旧错误状态残留也已修复，原失败在 `index-status-red.txt`。

`memory-final-local.txt` 中三项 LanceDB 写入失败来自过长的隔离库路径。使用短命名空间复测通过；没有修改产品存储路径，也没有修复或隐瞒 LanceDB 原生库的长路径限制。

因批准方案明确要求完整引用和英文提示词，仅相应调整原测试中的“省略引用正文”和中文定位标签断言；其余内容、表格、图片及来源检查保留。原用例副本与失败输出均留存。

### 真实请求

使用项目现有 `.venv` 和真实配置服务；只将测试记忆隔离到用户 `.redlotus` 下，项目与产物在 E 盘。测试模型、RAG 参数与有效上下文容量未为通过而调整。

- `real-source-v1`：失败。感知构造了错误的用户证据 ID，随后用户证据读取入口也缺失。已提供准确 ID 列表并补齐读取，保留原错误。
- `real-source-v2`：失败。重复主动记忆只查询、没有更新出处。补充明确的生产契约后重测。
- `real-source-v3`：5 个真实用户回合、18 个实际模型请求。L2 第一次创建，第二次更新同一 ID 至版本 2，记录两份独立来源；未出现运行日志 WARNING/ERROR。真实文件写入、回读和查询成功。原测试将末尾换行误判为额外内容，未覆盖原报告；`real-source-v3-independent-check.json` 独立核验确为要求的两行。
- 此小样本主 Agent 输入 114,867 Token，缓存命中 105,216 Token（约 91.60%）。只说明本组结果，不能代替三个环境的全量缓存门槛。感知与标题单独保留用量。
- `real-compression-v1`：实际 `/load → 对话 → /compress → 退出 → /load → 追问`。压缩用时约 10.36 秒，实际参数 `max_tokens=131072`、`reasoning_effort=max`。重载保持原 ID、文件、计数和 system 快照；酒店最新值 680 元/晚、3 晚、两人、交通 210 元、2026-10-03 及禁止订票/计算总额的约束保持正确。该测试是**手动压缩**，不证明自动阈值已真实达到。
- 完整压缩请求审查稿：`WorkDatabase/runtime/config-compaction-memory-20260920/compressor-request-review.md`；原始 HTTP body 及摘要在 `real-compression-v1`。审查稿不含认证请求头。

60 轮真实窗口验收仍在进行，尚未计为通过。第一次整理清单任务出现范围扩张及将静态检查写成“已核验”的质量问题，保留在 `real-windows-v1`。第二次驱动过早提交补充，当时尚未进入加急接纳阶段，实际被登记为普通回合；该次不作为加急通过证据。`real-windows-v3` 等待实际接纳状态后重测相同题面并保留一次加急澄清，当前尚未完成。

### 尚未通过的门槛

- 必要配置补齐与五种结果配置化；没有旧全局配置时的完整启动及权限等价。
- 60 个真实回合的三个自动窗口、L1→L2 真实提炼、纠正遗忘及跨项目全链路复测。
- 921,600 Token 实际自动压缩触发及数值保真；当前元数据解析容量为 1,024,000，未降低阈值。
- 本轮最终版本的日常 Miniconda pip、实际 onedir 及原三环境完整验收。
- 既有结构超标和全量测试收集问题。

未提交、未合并 master、未发布。失败证据保留，正式配置与个人记忆不参与破坏性测试。

## 2026-09-20 用户指出凭据问题后的状态

- `real-windows-v3` 已完成 60 个真实用户回合，在第 20、40、60 回合各完成一个自动感知窗口；第 19 回合退出恢复检查已完成。仍需独立核验全部记忆质量，不能据此宣布全部验收通过。
- 确认测试驱动曾向六份 private-config 文件复制真实凭据；第一版测试的工具读取使凭据进入真实模型请求。此为必须单独披露的测试安全失误，不能被功能通过结果覆盖。
- 已移除六份测试配置中的凭据、脱敏受影响测试会话，并禁用相关测试驱动。目前没有本轮测试进程继续运行。
- 事件范围、证据和处理见 `config-compaction-credential-incident.md`。尚未恢复真实 API 测试。
