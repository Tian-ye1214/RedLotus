# 会话验收口径冲突

本轮原始失败已保存到 `WorkDatabase/evaluation/session-refactor-20260916/all-v1.txt`。
不得将下列旧测试失败记成已通过。结构限制和未覆盖的功能也继续计为未通过。

| 旧测试要求 | 本轮已批准设计 | 替换后的验收 |
|---|---|---|
| 退出或 flush 消费不足一窗的事件 | 不产生不足 20 回合的收尾感知 | 退出后保留未消费回合，自动任务数不变 |
| 启动扫描旧项目状态，恢复未结束事件并触发感知 | 新建、加载本身不触发感知，只消费当前 session | 19 回合加载后仍为 19，下一真实回合封第一窗 |
| 复制旧会话、生成迁移备份 | 旧会话删除，不迁移、不备份 | 不导入旧会话计数；正式记忆、配置和引用保留 |
| Worker 和感知分别保存完整 JSONL | session 只有一个 JSON，不保存子 Agent 完整对话 | 主对话保留工具返回证据；子 Agent 累计 usage 保留 |
| 按旧私有属性读取 turns/jobs 文件 | 回合、任务状态写入同一 model_messages.json | 从新存储读取相同状态，原结果断言不变 |

用户再次确认：“退出时直接退出，以新口径为准。删除旧测试。”
据此删除旧迁移、收尾和启动触发断言，替换为表中本轮契约；原失败记录保留。
文件引用、审查、diff、goal、FIFO、取消、凭据与路径权限等行为没有更改授权，
这些测试的功能断言必须保持。

自动审批曾因可能删除原有断言、改变覆盖范围拒绝批量替换。
获得上述明确授权后，仅执行对应规则替换；测试是否通过仍以新的执行结果为准。
