# 本轮测试凭据事件

日期：2026-09-20。范围：配置精简、压缩与记忆整改的本轮测试；不含凭据正文。

## 已确认事实

- 测试驱动把 `config.settings()` 的合并结果完整序列化到六份 `private-config.json` 类文件，包含 `API_KEY`、`SILICONFLOW_KEY`。这些副本不应创建。
- `real-windows-v1` 将其中一份配置放在被测 Agent 可读的项目目录。Agent 的 `read_file` 返回了该文件，工具结果包含凭据。
- 随后的模型响应具有服务端响应 ID 和实际输入用量，证明该工具返回进入了真实模型交互。目标服务为 `api.deepseek.com`。不能将本次事件描述为仅本地落盘、没有外发。
- 含凭据的另一段命令返回也保存在同一份测试会话文件。根因是测试驱动复制完整配置，并把认证材料暴露给了被测工具；不是用户授权模型读取凭据。
- 本轮没有提交、推送或发布；该 runtime 目录没有被 Git 跟踪。

## 已处理

- 核实本轮测试进程已结束，未继续发起请求。
- 从六份测试配置中删除凭据字段，不保留含密钥备份。
- 对受影响的一份测试会话文件进行脱敏，并标明它已不是未经修改的原始请求证据；事务校验随脱敏内容重新计算。
- 禁用五个相关真实测试驱动，防止再次运行完整配置落盘路径。
- 复查本轮 runtime 文本产物，未发现这两份凭据的原始值残留；已检查的两个本轮测试记忆根目录未发现相同字节值。此检查不等于对整台机器、历史 Git 或服务端保存情况作保证。
- 正式配置和个人记忆未修改。

本地清理不能撤回已经发送到模型服务的内容。应由用户轮换本次暴露的模型与 RAG 凭据；本轮不擅自变更正式配置或账户凭据。

## 后续约束

`private-config.json` 不得包含任何实际凭据，不得作为模型上下文或上传材料。真实测试驱动须先消除配置复制，并验证请求正文与产物没有凭据，之后才能恢复 API 测试。不得使用脱敏后的历史文件重放来假称原始测试通过。

详细的无密钥清理清单位于本轮 runtime 的 `credential-incident-inventory.json` 和 `credential-incident-remediation.json`。
