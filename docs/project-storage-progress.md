# 项目存储收尾执行记录

用户批准方案：2026-09-17，保留 LanceDB，所有记忆数据库使用用户 `.redlotus`；项目会话/日志在项目 `.redlotus`，引用/依赖/缓存/产物在项目 `WorkDatabase`。

- [x] 恢复 LanceDB，保留隔离、纠正、遗忘和索引版本检查。
- [x] 完成启动/加载及长路径回归；扫描不改正文，实际加载才提交尾部恢复。
- [ ] 最终版本本地检查、三批真实业务、wheel/onedir/onefile 验收。
- [ ] 核实归属后清理测试产物，提交并按验收门槛合并推送。

2026-09-17 用户最新指令：尽快完成并立即合并 `develop`。停止新增验收和构建，按当前结果交付；长期验收、PyInstaller 与既有结构限制仍明确保留为未通过。已按指定清单删除 65 个 E 盘测试目录及 3 个 D 盘旧基准目录，未删除正式或归属不明的数据。

沿用现有隔离工作树 `codex/project-storage-sqlite`，不覆盖原有会话与 TUI 修复。模型及 RAG 服务参数不变。既有结构门槛单列，必要源码净增已有授权。

任务边界：主代理负责配置、记忆及真实验收；协作代理分别负责分发依赖、长路径执行、TUI 加载回归；各自文件范围分离。安装和全套回归不并行运行。
