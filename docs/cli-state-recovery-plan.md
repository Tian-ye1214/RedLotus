# CLI 状态与会话事务修复

用户批准基线：develop@fb1260ff；实现分支 codex/cli-state-recovery。

## 实施约束

- 保留原功能、普通 FIFO 与 Ctrl+Enter 内循环；不修改模型、RAG、输出长度、压缩阈值、会话提示词快照。
- 一个 session 一个 model_messages.json，增量事务提交，失败恢复最后完整批次；工具结果未知不推断成功、不自动重放。
- 手动压缩期间输入等待；摘要生成不写共享历史，检查归属后先保存再应用；重置或取消使旧结果失效。
- 清空解绑会话记忆；加载区分新建、恢复、取消。用户后续确认：项目切换期间锁住输入框、保留草稿；失败留在原 session，不新增待发送列表。
- ENOSPC 时从旧到新清理超过七天的可删除会话，然后清理未使用的程序临时文件/缓存；只在相同卷处理，不删源码、依赖环境、产物、正式记忆、引用原件。仍失败则暂停，下一普通输入先重试保存。
- 修复 LTM、记忆 ID 业务错误、项目内长工具输出与帮助显示。
- 单次固定验收期限 600 秒，真实模型与辅助故障注入分开记录；既有结构超标不掩盖，未通过不合并。

## 工作分配与记录

| 工作 | 所有者 | 状态 |
|---|---|---|
| 输入归属、三态选择器、询问框 Ctrl+Enter | cli_input_repair | 已修复，14 项定向功能通过；按新确认口径锁输入 |
| 压缩候选、命令修复、缺失工具结果恢复 | cli_compression_repair | 已修复；包含于 58 项定向通过 |
| 记忆解绑/读取错误、工具结果产物 | memory_tool_repair | 已修复；包含于 58 项定向通过 |
| 会话事务、磁盘不足处理、运行整合 | 主 Agent | 58 项定向通过；切换失败保留状态 5 项通过 |
| 全局配置备份、固定真实验收、交付 | 主 Agent | 222.30 秒真实批次已完成；配置指纹未变；全量回归 374 通过/2 结构失败，416.41 秒 |

基线工作区干净，复用现有隔离 worktree，不变更 develop。基线 core 有 10 个 Python 文件，原五文件结构检查不通过，其余结构检查通过；本轮不放宽断言。

修复前现场：E:/代码/Agent/WorkDatabase/evaluation/cli-audit-20260916-224040。
本轮证据：E:/代码/Agent/WorkDatabase/evaluation/cli-state-recovery。

交付结果见 `docs/cli-state-recovery-results.md`。结构与缓存命中率未达标，保留分支与所有失败证据，不合并 `develop`。
