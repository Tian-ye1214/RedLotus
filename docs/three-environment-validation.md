# Three-environment final acceptance

Baseline: `develop@1df51729`. Status: **in progress; not approved for master**.

## Agreed scope

- Existing project `.venv`, daily Miniconda pip installation, and PyInstaller onedir.
- Each environment: 100 main-session turns and 20 cross-session turns. 360 planned user turns in total; model requests and corrective reruns counted separately.
- Three real automatic compressions per main session, five perception windows at turn 100, three at turn 60, and restore across turn 19/20.
- Current model/RAG settings and compression thresholds preserved. Main/ordinary-worker reported cache hit ratio above 90% in each environment.
- First-use config.json supplied without credentials, model selections or private service addresses; interactive setup before Agent creation. Existing configuration is preserved.
- Auxiliary tests and actual model/terminal results reported separately. Two known source-layout failures remain explicit exceptions.

## Execution ledger

1. Confirmed clean develop baseline, existing interpreters, and published upgrade starting points during planning.
2. First-use regression tests added before implementation; packaging and fixed scenario manifest delegated as bounded independent tasks.
3. First-use/configuration helper regression: 41 passed. Packaging: 9 passed, including actual sdist-to-wheel build; installation helpers: 31 passed. These are auxiliary checks, not real model acceptance.
4. Actual project `.venv` + `main.py` first-use terminal run exposed an Escape cancellation defect: builtin input swallowed Esc. Replaced it with the existing PromptToolkit dependency's explicit Escape binding; repeated the same real startup and Esc exited with code 0 before Agent creation. No credentials entered or model calls made.
5. CLI regression: 48 passed, one failed because its native LanceDB fixture was accidentally on the E: exFAT volume. Full auxiliary rerun uses the already agreed C: isolated Lance test namespace; application backend or storage parameters were not changed to conceal the failure.
6. Source and global formal configuration SHA-256 remain `28341cf1f203148d5752a1117a306d07a44f7aeb04c40ebfd33d47364d1ef86d` and `3ad77345d5ea65dffc185074d8b04230bb1d32d5f4bc4c6ede9fa8b4b8991751` respectively.
7. First full auxiliary run: **567 passed, 8 failed, 585.01 seconds**. Two failures are the agreed source-layout exceptions. Others cover Ctrl+Enter timing, Anthropic SDK HTTP client compatibility, optional RAG indexing, non-TTY startup, a long Lance lock path, and an observer test collected while its implementation was being corrected. All are retained pending targeted rechecks; no claim of a green full suite.
8. Real source-terminal calibration: 5 business cases plus 1 correction, 6 actual completed turns. M004 incorrectly calculated `450 + 680 + 130` as 1450, which propagated into M005's plan file. No compression had happened. A user prompt requesting tool verification caused RedLotus itself to create/run a check and correct total to1260 and remainder to2390. No target-project code was supplied by the evaluator. See the E-drive `calibration-result.json` and original session.
9. Tightened the existing calculation instruction to require actual calculation and a reread of numerical deliverables. A fresh real source TUI session repeated M001–M005: M004 now invoked a script and correctly reported1260; M005 generated and checked its own budget document with remainder2390. These observations do not complete the360-turn gate.
10. Added only the missing test-side resource sampler `psutil7.2.2` to the existing project virtual environment; confirmed its import path is `.venv/Lib/site-packages`. Application dependency declarations and model parameters were not changed.
11. No merge, push, or public release performed. Actual pip upgrade/reinstall and onedir acceptance remain pending.
12. Installed public PyPI 1.0.0 in the daily Miniconda environment using its downloaded official wheel. CMD resolves `D:/Software/miniconda/Scripts/redlotus.exe`. Two actual startup attempts failed before a model response: the old package calls the removed `OpenAIModelProfile.from_profile` API. This legacy failure is not a passing upgrade result. The old release ignores current explicit data-path overrides and created a small AppData config/log footprint; retain its owned-file inventory for cleanup, rather than attributing that behavior to the candidate.
13. The first onedir build returned exit code 0 but was **rejected**: analysis reported missing `redlotus.core`. The installed 1.0.0 regular Python package shadows the source namespace package. The candidate wheel must replace the old installation before building again; an executable existing on disk is not evidence of a usable package.
14. Fresh source setup completed all role/model, hidden credential, and RAG prompts. Subsequent M001–M007 submissions all failed with zero model responses because the sanitizer removed the public `model_metadata.url`; fallback context and configured output both became393216. These seven failed submissions are preflight failures, not360 successful planned turns. Repair preserves the source metadata parameter; model/output/compression settings remain unchanged.
15. Public GitHub v1.0.1 onedir ZIP downloaded and checked against published SHA256SUMS: `1ad68eef936ada61eff4f42b0da47d7cdef2906dfcfa6ea417941630327543ee`,320502097 bytes. It has not yet been run. No onefile build/download is part of this round.

16. Candidate wheel r2 built and audited, then actually upgraded daily Miniconda from public1.0.0 to1.0.1; public1.0.0 could not create a successful baseline session because of its SDK incompatibility. Candidate first-use and M001–M019 ran through real CMD redlotus. The old-version session-preservation gate remains unproven.
17. Public GitHub v1.0.1 onedir completed one actual conversation and exit0, with one usage response (input6466/output130). Candidate r2 onedir built in372.382 seconds and passed archive/resource checks; its3332-file ZIP was safely extracted. These static checks alone are not candidate EXE acceptance.
18. Source final controller failed at a journal atomic replace after M011. Last saved session has11 completed turns and no active turn; M012 delivery is uncertain, and was not counted as completed. This is a test-driver failure, with original evidence retained; recovery must inspect the actual application history before any resubmission.
19. Latest full auxiliary suite:595 passed,2 skipped,3 failed in642.14 seconds. Two failures are agreed layout exceptions; the16-thread admission test failed once, then passed three unchanged isolated reruns. Its cause remains under investigation, not claimed fixed or dismissed as machine load.

20. Recovered the source controller after confirming that M012 text never reached application admission; it was submitted once in the restored original session. The subsequent planned19/20 exit and restore succeeded in all three actual runtimes. Session IDs stayed `7ceb4cbe75464f51919cc7bb58706b07` (source), `edcf8e9585e0447da7b84bf3119fc096` (pip), and `dbf2ce99c1e24f08b8dc880007e41564` (onedir). Loading and the19-turn exit created no automatic perception window.
21. First automatic20-turn windows completed and indexed in all three: source184.526s total/180.509s model API; pip205.571s/199.974s; onedir346.233s/337.515s. Each is one scheduled job, with respectively3/3/2 API requests for its internal workflow. All consumed20 new turns and0 overlap; their stored failure lists are empty. These timings do not by themselves prove the quality of every extracted record.
22. Actual public-v1.0.1-to-candidate onedir replacement restored session `93f2a246917f416da1abea066b2d7480`, recalled its temporary marker, continued the same file from1 to2 turns, and preserved the isolated configuration hash. Both processes exited0 without unexpected runtime warnings. This is an artifact replacement at the same version, not a version upgrade; it is separate from the360 planned business turns.
23. Official Playwright browser installation twice failed on E: exFAT with its own stale-lock check. The same official archives were downloaded, path-validated and extracted to the exact expected E-drive cache; existing source and daily Miniconda then both launched and closed Chromium successfully. Keep the CLI installer failure and manual-install limitation visible. No third-party lock code or application browser implementation was changed.
24. Actual M018 urgent batches sent six separate Ctrl+Enter sequences per environment and remained within their existing outer turn. Actual panels in all three showed `Agent Running 2` while a delegated blog Worker was active. The earlier onedir panel observation showed1 before that Worker had started; it is retained and not relabeled as a worker observation.
25. Source M001–M008 fresh-session corrective rerun completed8 extra turns and41 actual responses. M001 now delivered the requested answer. M006 still applied the earlier temporary hotel override instead of the fixed CSV-only assertion; M008 still guessed the ambiguous object before asking. Both remain partial failures. Added general coordinator instructions separating source-file calculation from scenario overrides and requiring clarification before an ambiguous state change; another identical-input source rerun is pending. Existing main-session prompt snapshots have not been rewritten.
26. Stage sample at source26/pip29/onedir24 completed main turns reports main+ordinary-Agent cache ratios98.198%/99.047%/98.119%, including cold requests. Respectively3/3/4 rows lack cache detail; some are suspected SDK-generated ToolSearch history records, pending a live provenance check. Do not discard unidentified records or treat these stage ratios as the final post-compression gate.
27. Candidate wheel/EXE r2 precedes the latest prompt corrections and usage diagnostics. They must be rebuilt after fixes stabilize and verified again. The360 planned turns, cross-session cases, three automatic compressions per main session, full command matrix, and final cache/resource gates are still incomplete. No master merge is authorized by these partial results.
28. A real Worker task traced the identity-less usage row to Pydantic AI's local deferred-capability `auto_load_` receipt. A narrow accounting filter excludes only that non-model record while preserving the conversation history and genuinely unknown responses. The same real task after repair completed with8 identified responses and0 unknown rows;18 focused regressions passed. Previously stored unknown rows were not rewritten.
29. Corrupt-configuration startup unnecessarily imported the model SDK, Agent and LanceDB before reporting the error. The original real Miniconda source-entry probe took15.823s. After deferring these imports until configuration succeeds, the identical probe exited2 in0.567s with the same file-specific error;24 first-use regressions passed. This verifies the source change only, pending rebuilt pip/EXE retests.
30. Three actual runtimes each returned an external401 for a deliberately invalid key and an external400 for a gateway-unsupported model, then successfully conversed after restoring the isolated test configuration. Failures stayed failed, created no perception job, and left the terminal usable. An initial unknown-model probe stopped locally because fallback context equaled configured max output; that preflight failure is retained separately, not counted as a server request. Known HTTP errors currently also dump a traceback into the user interface; a focused UI repair is in progress without lowering file-log severity.
31. After the startup import repair, a fresh actual source ConPTY run displayed the missing `models.coordinator.name` prompt without any bootstrap/input keystroke. Escape exited0; the fixture hash stayed unchanged, no global configuration or session was created. Complete launch/observation/cancel took14.926s. This is not a measurement of the prompt's first paint alone.
32. Read the actual native Lance records for the first window in all three isolated namespaces. Project aliases and automatic episodes exist with source-turn/reference IDs; the temporary travel conditions were not projected into the source core profile. Source's episode overstates completion of the original budget task that the independent evaluator rejected. Successful scheduling/indexing is therefore reported separately from memory-content quality; the record was not edited to manufacture a passing result.
33. All three main sessions reached40 with exact frozen-prompt mapping and no added outer turns for M018's six urgent messages. Source also received its M024 image-title correction inside M028. Source has two observed automatic compressions (M025 andM037); pip has one at the beginning ofM033, and onedir one associated withM024. These are intermediate counts, not the three-compression final gate.
34. M039 first delivery failed in all three: the model removed existing wording instead of producing a new candidate and awaiting the evaluator's UI decision. The original phrase “我选择撤销” is ambiguous about whether cancellation has already been instructed. Existing pending whole-file changes and incomplete12k ANSI captures also prevent proving a targeted UI undo. Preserve the failures; retest with an explicit wait-for-UI correction and visible path/hunk verification. This evidence does not establish that whole-file review grouping is an application defect.
35. Actual40-turn exits released the three application processes. Pip and onedir's second20-new/3-overlap windows were complete and indexed; source exited while its second window was still in progress, leaving a durable unfinished job. That source job must be checked on restore, not counted as successfully produced or as a new schedule. Same session IDs and recovery files were retained.
36. A source corrective M001–M008 run with the updated coordinator instructions completed eight exactly mapped real turns: source CSV calculation, temporary scenario calculation and ambiguous follow-up now meet their separate assertions. Runtime logs had no unexpected warning/error and exit was0. A prior corrective run duplicated its first message despite only one test-side submission; its input-chain cause remains unconfirmed. A successful later run after waiting for the input screen is not a fix for that early-input condition.
37. Daily CMD discovery succeeds in the actual host user's persisted PATH without changes. An earlier restricted-process registry probe could not see the same user PATH; that environment observation is retained and is not reported as a product PATH defect. Separate real pip `/load`→Escape→Ctrl+C probes, both adjacent keys and visible-modal-close sequencing, exited0 without creating a session; the earlier hang remains unreproduced, not silently discarded.
38. Candidate r3 wheel/sdist build and resource/credential checks passed, but installation was held when review found a first-use defect for an entirely absent model-role object. Required-role discovery and name prompting are under repair. Artifact existence/static checks are not installed-runtime acceptance.
39. Full auxiliary batch r4 finished in414.47s:617 passed,2 skipped,2 failed. Both failures are the agreed source-layout exceptions. The previously intermittent thread-admission test passed unchanged; its earlier failure is retained. A later missing-role fix separately passed26 first-use tests and is not retroactively attributed to this already-running batch.
40. Corrected the test-side stage cache aggregator: main-Agent usage rows carry `turn_id` without an explicit role, as documented by `SessionFile.save_context` and the application's `read_usage_messages`. The earlier “foreground” stage ratios accidentally included only workers; their report is preserved as `worker-only-stage-superseded.json`. At40 turns the corrected main+ordinary-worker reported ratios are98.4457%/98.7849%/98.2724% (source/pip/onedir), with no input-vs-hit/miss mismatches. The3/3/5 older unidentified zero-usage rows remain visible; no session counters were changed. These are still stage results, not the final120-turn gates.

41. Candidate r4 wheel replaced the daily pip installation after a test-owned, unsupported `redlotus --version` probe was found holding the launcher open. Only the verified probe process tree was stopped. The failed pip attempt and owned uninstall stash inventory are retained; a subsequent reinstall and `pip show` completed cleanly. This was a same-version artifact replacement, not a new public release.
42. A missing entire Worker object now prompts for its policy and exact `models.worker.name`. Actual source cancellation preserved the configuration byte-for-byte; a second source run accepted explicit `=coordinator` reuse and reached the normal UI without creating a session. Actual daily CMD pip cancellation also passed with a correctly prepared fixture. An earlier pip probe omitted every other role's model name and is excluded as an invalid fixture, not reported as an artifact failure.
43. A second real tight Escape/Ctrl+C sequence reproduced a modal keyboard issue: Textual parsed the adjacent bytes as Ctrl+C without Escape, while the selector had no local Ctrl+C binding. The user confirmed that Ctrl+C in this selector must cancel it and retain the original session. A focused binding fix and regression are in progress; global Ctrl+C behavior is unchanged.
44. Source main-session restoration at40 exposed a separate real defect: the UI displayed “ready” but subsequent text was not accepted. Local UI reproduction failed for restore, new and cancel alike because disabling the input removes focus and re-enabling it did not restore focus. All three focused tests passed after restoring focus on the disabled-to-enabled transition. The failed live submissions were not counted as turns; actual terminal revalidation is pending.
45. Real LibreOffice conversion of old DOC/PPT/XLS failed during private-profile cleanup after the converter had exited. A generated cache path reached261 UTF-16 characters; ordinary Python deletion failed and an extended-path deletion of the same owned directory succeeded in47ms. The local fix retains separate project-local profiles and uses standard-library extended paths only for cleanup. Actual DOC/PPT/XLS conversion, marker extraction and process/profile release passed after repair; rebuilt installed-runtime validation remains pending.
46. The source input-focus fix passed a real restoration of session `7ceb4cbe75464f51919cc7bb58706b07`: M041 text was visibly accepted and its urgent correction stayed in the same outer turn. A later independent actual restore resumed that same file at41 and M042 completed. This updates item44's pending live recheck, without relabeling its earlier dropped submissions as successful turns.
47. A test-driver race was independently reproduced: reader and timer removed successive ANSI batches under a lock but appended them to the transcript after releasing it. Both append operations now remain inside the existing lock. Two deterministic regressions failed before repair and passed afterward; the combined terminal/focus/selector auxiliary set collected37 tests and passed. Older terminal evidence is retained; a stale or reordered display alone is not proof of an application state change.
48. Pip main M043 and M044 both failed before a foreground model response in `compact_request_messages -> _call_compressor_llm`. Their actual logs identify a16384-token compression limit; neither failure is a successful business turn. The same44-turn session was preserved and `/exit` completed with code0; M045 has not been submitted. A separate real replay of its captured compression input reproduced the failure in70.453s: input31486, output16384, all16384 reported as reasoning, `finish_reason=length`, no text part. The model configuration was not changed; a focused compression-prompt retest is in progress. This replay is diagnostic, not a completed main-session compression.
49. The unchanged r4 onedir artifact and selected main-test configuration completed an isolated real chat in0.91s and exited0 on2026-09-19. The two earlier main-session connection failures were not reproduced. Their cause remains unconfirmed; a single later success does not establish a product fix. Candidate r6 wheel/sdist passed resource and secret-exclusion checks, and r6 onedir built with exit0 in367.713s; installed-runtime acceptance remains separate.
50. A prompt-only compression correction was replayed against the exact captured input from item48. It returned a complete eight-section checkpoint in59.406s, with31404 input and11771 output tokens (8466 reasoning,3305 text), finish reason `stop`. The6357-character summary retained the current game task, the review undo decisions, unsynchronized travel artifact and verification gaps. Thirteen compression-related auxiliary tests passed in8.30s. Daily pip restoration and retesting of the original failed business cases are still required; the diagnostic summary was not inserted into the main session. Wheel r7 includes this correction and passed the same source/resource/secret checks; its SHA256 is `1f0b6f3d0fc9bebfd345b627d06b38c17e371ea8b0fc6e25e2cc40e460de08b3`.
51. Daily pip r7 restored the same44-turn session and retried M043 as a separately counted corrective turn45; automatic compression failed again at16384 before an actionable response. Original M043/M044 failures remain, and no later business case was queued. The installed normalized prompt hash matches the tested revision; its raw-file hash differs only because file bytes and normalized prompt text are distinct measurements. This disproves a stable prompt-only fix. A second diagnostic wording produced14851 reasoning tokens plus1533 text tokens, then truncated after five of eight sections; that wording was reverted and its evidence retained. Configuration inspection confirms `compressor.thinking=enabled`, `reasoning_effort=max`, `max_tokens=16384`. Permission to change only compressor thinking has been requested; no configuration was changed while awaiting the answer.
52. Clarification of items48/50: the diagnostic input was reconstructed read-only from the persisted session and the production compression builder, then held identical across diagnostic replays. It was not captured at the original failed HTTP boundary, so it cannot establish byte-for-byte identity with those app requests. Boundary inspection after the next failure found the retained context about9242 estimated tokens against a630784-token input budget, supporting tail retention; original failed calls did not preserve per-request compressor usage and their reasoning/text split is unknown. The diagnostic all-reasoning measurement applies only to its measured replay.
53. In a separate source ConPTY run, both standalone Ctrl+C and adjacent Escape/Ctrl+C cancelled a visible selector and kept the original session ID, file bytes and seven history entries. Distinct unknown-slash-command probes were accepted afterward, proving usable keyboard focus without model calls. These expected command rejections are separate from runtime faults. Evidence includes real ANSI VT replays (not desktop screenshots), exit0 and an unchanged formal configuration. This closes the source selector interaction retest; installed runtimes remain to be checked.

54. Review reproduced a retrieval race when embedding configuration changes during an in-flight query: the old vector could reach a different model's table, including one with a different dimension. Each retrieval now retains its selected model/database pair. Two event-controlled regressions failed before the fix and passed afterward; the focused RAG/optional-RAG suite passed14 tests using the permitted isolated native database location. These are auxiliary concurrency checks, not live service acceptance or a change to memory scheduling.

55. Actual early-input A/B probes reproduced two submissions from one nonempty draft after a startup bootstrap Enter. Instrumentation recorded two submit actions, not a CRLF/urgent pair; the CRLF hypothesis was rejected. Local regressions then reproduced duplicate submission and deletion of a newly typed draft. Moving the clear to `action_submit` fixed those local cases, but a real two-message burst still merged both texts because submit keys bubbled to the App after later characters had already reached the Input. The independent memory-quality M018 replay likewise merged its fifth/sixth urgent inputs, despite answering all six requirements. A second narrow fix handles submission keys in the Input's own ordered event queue and posts an immutable value/priority event. Full local and real terminal retests are still pending; neither the old business answer nor the intermediate clear-only fix is counted as a pass.

56. A fresh source quality replay completed the frozen20 outer inputs in session `d6cadd7dbf5a4d7d969c5e18a7d79883`, including an actual19-turn exit/restore. One automatic window consumed20 new turns and0 overlap, made2 model requests plus4 RAG searches, and indexed episode `079edf94221e3c18a56c7d1b15e0cabd` with an actual1024-dimensional vector. It correctly kept status `unverified` and recorded the unsynchronized historical travel file as unresolved; the current1150 total and temporary1260 scenario were distinguished. M020 also retrieved/read the explicit project memory in the restored app. Perception took188.719s, including184.249s API and4.469s preparation/save/index work. Logs contain no unexpected warning/error and the actual app/driver exited0. This verifies the perception-quality correction for this source replay; M018's merged urgent messages remain a separate failed input criterion and the20 replay turns are additional tests, not part of the360 planned main/cross-session total.

57. The installed r7 daily CMD entry `redlotus` passed standalone Ctrl+C and adjacent Escape/Ctrl+C selector cancellation in a separate test project. Both probes restored keyboard focus, and the copied test session ID, one recovery file, history and file hash stayed unchanged; the local unknown-command probes made no model request. Main-test configuration hash `28c0…b2c4` refers to the isolated pip profile, not the operator's formal configuration. Actual exit and absence of the probe's processes were verified. Evidence is `pip-picker-ctrlc-conpty-20260919-r7/result.json`; its rendered terminal grids are ANSI VT replays, not native desktop screenshots.

58. The input-boundary repair passed49 local tests. A subsequent instrumented real probe appeared to bypass the new public `on_key`, but the diagnostic wrapper had injected a private `_on_key` onto the same class, which Textual chooses ahead of `on_key`. That probe changed production dispatch and is invalid for judging the repair; it is retained and corrected in the audit rather than counted as either a product failure or pass. Unmodified-path terminal and model-backed six-urgent-message retests remain pending.

59. The unmodified source entry then passed the original model-backed M018 retest in the same isolated quality session as a separately counted21st turn: outer request plus six distinct Ctrl+Enter inputs persisted as seven ordered originals. All six admission/consumption IDs belong to turn `1217ddf7422d4038bfad6525ba8f40f8`; U5/U6 hashes stayed separate, and tool calls/returns paired correctly. The actual answer met the six constraints. Loading and this extra turn left the automatic-window count at1 and consumption position at20. App/driver exited0. This closes the source urgent-boundary defect; installed pip/EXE require the rebuilt fix.

60. The first checkpoint-deduplication regression covered only an in-memory SDK message. Read-only reconstruction of the saved pip session showed that its top-level metadata had not survived persistence, while `TextContent.metadata` still identified the checkpoint. Reusing one helper for both supported metadata shapes fixed the restored case; the excerpt now excludes the internal checkpoint while retaining identical real user text. The reconstructed payload decreased73550→66977 characters. Twenty focused compression tests passed. With unchanged model settings, one real diagnostic then completed in58.578s:28191 input,12967 output (9947 reasoning), eight complete sections and finish `stop`. This diagnostic does not apply its summary to the main session and does not yet prove stable installed-app recovery.

61. Full auxiliary batch r8 completed in677.16s:636 passed,2 skipped,3 failed. Two are the agreed layout exceptions. The LF urgent test inspected the queue after widget idle without waiting for the CLI admission task; its original failure is preserved. The test now waits, under its existing five-second bound, for the same exact admission counts before making all original assertions. The subsequent complete entry/compression subset passed42 tests in48.19s, including the corrected persisted-checkpoint regression. The full batch is not relabeled green, nor does its earlier imported code prove the later checkpoint repair.

62. Candidate r8 wheel/sdist built in26.756s and passed all8 resource/source/credential checks. Wheel SHA256 is `fd6734cdb79c108d5ae5aac981e12e76704b969ae82c3eb8224d04dbb11f5888`; sdist is `f8b71cad35c4f05f6c18d3bddea6ffaedcc3b28dc4b9e764d625e76c577e0c24`. It replaced the daily Miniconda1.0.1 installation with `--no-index --no-deps`, leaving application dependencies unchanged. This is a candidate artifact replacement, not a public version upgrade. R8 onedir then built with exit0 in321.150s; installed-runtime fault retests are pending. Formal source/user configuration hashes remain `28341…ef86d` and `3ad773…1751`.

63. Installed r8 daily pip restored the original45-turn session and submitted the original M043 as corrective attempt2, actual turn46 (`cdad5ebb3ec145a391f81b25521fe0f6`). It failed again before tools, at `compact_request_messages -> _call_compressor_llm`, with the16384-token limit exhausted before usable text. Evidence is `pip-r8-m043-corrective2-failure-evidence.json`. The persisted foreground failure contains only zero usage placeholders; the actual compressor payload and nonzero usage were not captured, so neither the diagnostic's measured reasoning split nor a zero-cost claim applies. No M044 or later prompt was sent afterward. The passing diagnostic in item60 does not close this installed-app failure. Settings remain unchanged; the previously requested compressor-thinking adjustment is still awaiting the user's answer.

64. Full auxiliary batch r9 on the actual project `.venv` completed in684.74s:637 passed,2 skipped and the2 previously accepted layout exceptions. The failures remain `test_each_application_file_has_at_most_500_effective_lines` and `test_each_board_contains_at_most_five_python_files`; this is not an all-green suite or real-model acceptance. Archived log `reports/auxiliary-r9.log` SHA256 is `7B76B690DAE1953B315C779FBAAA7818B2D0FCD7A8F3B32B6971830C08353541`.

65. The apparent r6/r8 EXE M046 input stall exposed a test-driver error: its mailbox still contained literal `@{{fixture:game_playtest_csv}}`. The fixed corpus declares an existing117-byte CSV, but the driver had omitted fixture expansion. A read-only call to the production path parser resolves the literal to a nonexistent project path; the existing CLI rejects such references and restores editable input before starting a turn. The old dispatches remain invalid test deliveries, not evidence of a product input defect. The terminal driver now rejects unresolved fixture placeholders before any PTY write; the regression failed before the guard and all32 terminal-driver tests passed afterward, preserving nonstandard path syntax. Correct-path real replay and reconciliation of an independently observed persisted active-turn field are still pending.

66. The existing no-model first-use/error matrix was repeated against current source, daily CMD pip r8 and actual installed onedir r8:12/12 checks passed (5/4/3 respectively). Evidence `reports/first-use-errors-r8-matrix-retest.json` covers specified missing-field/malformed-JSON/cancel paths and absence of session artifacts; it does not claim full first-use success or independently prove zero network traffic. The wrapper initially failed at import before any case because its dynamically loaded module was not registered for dataclass processing; this harness failure is retained separately from product results. No formal configuration or memory was modified.

67. Correctly resolved EXE M046 completed in r8 as actual turn48 with `success` and a persisted input SHA matching the expanded CSV prompt (`de77c52c…`), rather than the template hash (`57c33c…`). The earlier active-turn alarm also came from an incorrect evidence query that selected an older nonnull metadata value: the production read-only SessionFile loader showed idle47 before the valid replay and left file bytes unchanged. The literal-placeholder dispatches remain test-driver failures, not product input failures.

68. Source M044 feedback2 passed independent real-browser keyboard checks: P froze ball, paddle, score and brick state; P resumed movement; R reset score, bricks and serving state. No game state was injected. The saved screenshot hash is `265B03D1787C38A83F5B8A248891AA270E6AD6F4E261247F847DDB21D8AD1151`. M058 was initially inconclusive because tail-only ANSI replay had lost the previous screen body. After one user feedback turn, fresh untruncated ConPTY frames showed an actual Y decision retaining the pause gate and N removing the snapshot debug field; independent file inspection confirmed both results. Main source now has61 actual completed turns for58 frozen cases, preserving corrective turns separately.

69. Source's third automatic perception job `220b69b83754de496810488222220776` consumed exactly41–60 with overlap38–40 and indexed six record IDs. It took396.004 seconds:387.503 measured API and8.501 other time. Source consumption remains60 after actual61, with exactly three automatic jobs. Record-quality review is separate. The second window's2761.678-second persisted duration includes intentional exit and later restore: `reports/source-window2-recovery-timing.json` measures2266.909 seconds between the pre-exit call ending and resumed preparation. Subtracting that observed gap leaves7.591 of its reported other seconds, but the original end-to-end duration is retained. One cancelled API call has no usage and is not treated as free.

70. An independent source CLI check found a genuine startup admission race: Enter could post an immutable input and clear the composer before startup disabled it; the later transition check then discarded that event. A deterministic reproduction using the public Input action and real workspace-entry method confirmed the lost command without private event-handler injection. A narrow startup-lock fix and regression are in progress. The successful delayed `/agent` replay does not erase the first failure, and r8 artifacts do not yet contain this repair.

71. Independent record audit found an eligibility failure in source's third window: existing blog episode `e753c872a468a93b907c2fbb4d7ac6cb` advanced version1→2 (Lance table snapshots6→7) with a changed body, but its sole added source turn was the first game request, not new blog work. Stable-ID update and indexing were correct; the semantic basis was not. The perception prompt now explicitly prohibits unchanged/reworded or overlap-only updates and requires a new turn that actually supports the changed fact. A real-evidence replay is being prepared; this prompt change is not yet a passed repair and no current memory record was edited to manufacture success.

72. Source frozen M063 failed at actual66 before any taskapp implementation with the same compressor16384-output-limit error seen in pip. Full untruncated terminal evidence and exact input mapping are saved in `reports/m063_failure_evidence.json`. M064 was not sent, settings were not changed, and this failed call is not counted as a successful third automatic compression. Independent game play verified keyboard, single-touch movement, scoring, pause/resume and reset, but final win/loss states remain unimplemented; the game is only partially delivered.

73. The terminal evidence collector now optionally maintains a bounded visible VT grid from the full received stream, avoiding loss of an old review body when only recent ANSI is retained. It records text/cursor fidelity explicitly, not original colors or a desktop screenshot; ANSI-only fallback remains available when optional pyte is missing. Standard PTY resize, fragmented-secret redaction and retained review-body regressions passed in the focused project-environment run:56 passed in44.18s, archived `terminal-grid-scaffold-20260919/focused-pytest-r2.log` SHA256 `d6c0685509435d8464420f3ea287ed6543fe6579e31e6ca5d810097ec03a4025`. Daily Miniconda's helper-only run passed25 and skipped7 optional visualization cases. These are test-tool checks, not application functionality or GUI acceptance.

74. Source first-use replay r2 completed the configuration wizard and atomic save, but its early S017 terminal delivery never became an application input. Persisted hashes show only S018 was admitted, producing one completed turn. The failure is preserved in `reports/first-use-s017-s018-r2-startup-input-evidence.json`; receipt IDs alone do not prove admission. The next attempt was stopped before chat while the startup lock repair was being finalized, and is not counted as a pass. Separately, a read-only scan of all265 versioned/nonignored repository files found no exact current configured credential values; ignored runtime evidence is outside that scan and is being audited separately.

75. The startup repair now disables the composer and load control from their first frame, retains the lock through preparation/workspace entry, and displays “正在准备会话…” instead of a premature ready label. The three targeted startup tests passed; the existing entry/load/transition set passed38 tests after its fixtures actually completed startup. Actual clean first-use replay is still required. The fix does not queue hidden drafts or change ordinary clear/project-switch semantics.

76. The real first-use capture exposed a test-recorder credential defect: ANSI control sequences separated a hidden-input value, then stripping those sequences reconstructed it in `plain_tail`. The recorder now strips and redacts before truncating plain text. A synthetic reproduction failed before the fix;57 focused tests then passed. Exactly five identified pip receipts were sanitized in place (seven matches, only `/terminal/plain_tail`), with original/result hashes retained in the credential-free manifest. An independent scan of677 readable isolated-run files, excluding only the authorized profile config, found zero remaining configured-secret values. Seven separately identified older source-wizard receipts were also sanitized; three contained key values and four only service addresses. No raw backups, session edits or formal-config changes were made. This is a local-artifact check, not proof about uncaptured network payloads.

77. Daily pip's real S017/S018 first-use pair completed two uniquely mapped turns, with correct isolated configuration provenance and no configured keys in the saved model session. S018 nevertheless required480.149 seconds,25 provider responses and55 successful tools, including38 command calls. It accumulated1,726,923 input and89,934 output tokens (82,889 reasoning). The model chose24 successive tool batches; there was no recorded transport timeout or failed-tool retry. Existing `/config` provides a direct redacted answer, but the model has no equivalent provenance-summary tool. This efficiency limitation remains reported; no new tool or configuration policy was added without authorization.

78. Candidate r10 wheel/sdist built in39.552 seconds and passed all8 source/resource/credential checks. Wheel SHA256 `9a002e7a1aa5ff78a6aff04e16a6d2ede648eef2ccc50a68ddf513fd5e00cb3f`; sdist `fbb7be15418708a19fd2fa0d5e81f9d576f5abf8488f92509e80ab9e48a46182`. The wheel was installed into daily Miniconda with `--no-index --no-deps --force-reinstall`; dependencies remain unchanged. This is another1.0.1 candidate replacement, not a public release or version upgrade. R9 was superseded before installation. R10 onedir and installed-runtime retests are in progress. Formal source/global configuration hashes still match the baseline.

79. R10 onedir completed with exit0 in334.638 seconds. Its100 source fingerprints match the wheel exactly, with no missing or additional fingerprint entries. Resource audit, safe ZIP extraction and actual EXE validation remain separate steps. In source's clean first-use r4, the post-wizard S017 was admitted once (`d529dd5af9ef4d038a5ade498efebc4c`) into session `73e105edecc44557a280be0ebbab420f`, with one completed turn and one usage response; S018 was not pre-submitted. Black-box ConPTY evidence proves the observed ready boundary, not direct inspection of the widget's disabled property.

80. The first pip r10 S011 delivery was a test-driver error: text and Enter were written at process startup before the first visible ready state. The full terminal stream contains no submitted text, admission or reference-parser event, and the project has no session file. Later ready/empty screenshots cannot prove readiness at the earlier send time. Preserve this as an unadmitted driver delivery, not an application/reference failure or completed turn. The following delivery must explicitly wait for a fresh visible ready boundary and verify its unique admission; r10 application code was not changed in response to this false negative.

81. R10 EXE pre/post-extraction audits passed for3332 files; executable SHA256 is `2ddb4856b3e32595c84a771b0b7490ddf0fa1e29275063ded42ee585260bc8a1`, ZIP `5d17bdd72aab09330257ba2db3f9a0ca139d3eb6814efde86a26e06df00351cc`. The installed perception prompt matches source, and the original onedir main session remains at59 with the same file bytes and isolated-config hash before launch. This proves artifact integrity, not the upcoming60th-turn perception result.

82. Source clean first-use r4 completed the original S017/S018 in one new session, then exited0 with two completed turns, one recovery file, no automatic perception job, no leftover lock and no temporary config. S018 correctly identified the selected local JSON and exposed no configured API key, but required417.1 seconds,40 model responses and57 tools (49 commands). Thus the configuration-question overexploration is reproduced in both source and daily pip. The `/config` capture observed the source-order boundary but did not retain its full final redraw; exact UI-text verification is limited, while independent configuration provenance is recorded separately. See `reports/first-use-s017-s018-r4-summary.json`.

83. Read-only attribution of the11 historical zero-usage rows (source3, pip3, onedir5) could not establish an association with a retained SDK response, metadata or tool-call ID. All remain UNKNOWN. The narrow future `auto_load_` accounting fix does not justify retroactive reclassification, and overall historical cache coverage remains incomplete. No stored usage record or statistic was rewritten.

84. The terminal observer had a second test-harness defect: legacy `until_text` searched accumulated history, so a prior picker could satisfy a later load assertion before the current modal opened. Added explicit `until_screen` matching against the continuously rendered current grid; absent grid data is now an explicit observation failure. The reproduction produced two failures before the change, then46 focused terminal tests passed in13.15 seconds. Existing running drivers do not hot-reload this fix; their resumed cases use fresh snapshots and manual current-grid checks. Application code and r10 artifacts did not change.

85. Source S007/S008 completed in new session `b2a5eb78fe094d2782968b6bc9bbc55c`: the untrusted reference remained data, Chinese replies continued, no injected instruction was executed, and no memory job or record was created. S009/S010 completed separately in `197a7c5897b144eeb117f4cd4dfeda47`: a controlled exit7 remained a failed fact in requested project memory and later retrieval did not claim it repaired. Both pairs exited0. The supplied failure program was a frozen input fixture, not an evaluator repair to a RedLotus deliverable. See the two `reports/short_*_report.json` files. An earlier process-release observation based on denied CIM access was corrected; denied access is unknown, not proof of release.

86. Daily pip r10 S011/S012 identified the real random-image fact and restored its immutable reference in the same session `3132c227894d486cba0d3288d0c1cf3f`, with one recovery file and two completed turns. The unadmitted early startup delivery remains separately recorded as a driver error. Daily pip S015/S016 then completed in `74747e3db31d4c34894f82b4058f9610`: original plus five urgent inputs were preserved in order within one outer turn, the later D correction replaced B, and the next ordinary turn was admitted once. The short pair reported15 responses and91.0381% cache hits with complete usage coverage, zero unexpected application warnings/errors, exit0 and released owned PIDs. These are short-case results, not the per-environment final cache gate.

87. Source S019/S020 resumed the existing S017/S018 short session without added setup model turns. Fresh current-grid evidence first showed the picker, then Ctrl+C returned to ready. Before/after cancellation file hashes were identical (`f823f236…ef7dea4`), with session `73e105edecc44557a280be0ebbab420f` preserved. The two frozen prompts were admitted once as actual turns3/4 and answered in9.12/12.05 seconds. S020 used only this session's known facts, identified an unknown, and called no tools. One recovery file, four ended turns, no automatic perception job, and exit0 were independently checked. Report: `reports/source-s019-s020-real-cancel.json`, SHA256 `3e48bf61eee189fbbb36d58f519e6608e22b496ef130c2316b759911d38c6124`. Earlier history-based modal matches are excluded. S018's earlier read of evaluator provenance material remains an evidence-contamination limitation; its efficiency result is not upgraded.

88. Real r10 EXE M058 originally had no applicable game change to review; this remains a missing-precondition result. The single U1 clarification then produced candidates at actual61. Through the real TUI, `y` kept the game usage wording and `n` removed `TEMP-DEBUG.md`; independent hashes/absence confirmed the choices. The two unrelated inspection scripts were left unchanged and pending. Report: `runtime/onedir-r10-m058-u1-review-result.json`, SHA256 `8ea34d7e4fb0e9ba80ecbca18070233e6b72a16c0614c78794cf39b6933aa8c8`. No evaluator edited the game.

89. EXE third automatic window consumed exactly actual41–60 plus overlap38–40. Job `779c9570035f85a44a60fae6e2f10f65` took353.944 seconds total:345.773 seconds across3 real model requests and8.170 seconds elsewhere. Immutable raw/before/after snapshots support the old-blog update with actual43's new blog evidence; actual59–60 were not misreported as game changes or completed UI review. However, nine related new game episodes remain moderately fragmented. The independent audit's qualified result is not full memory-quality acceptance; aggregation diagnosis remains open. Reports: `reports/onedir-third-window-quality-r10.json` and `.md`.

90. The follow-up read-only aggregation audit downgraded the third EXE window's full memory-quality gate to FAILED: its own production reason says the game was split by subgoals into nine new records, contrary to stable-task aggregation. Existing similarity searches cannot see drafts in the same uncommitted batch. Delivery/control improvements, test creation and later regression overlap materially, while independently evidenced defects may reasonably remain separate. A narrow pre-submission grouping clarification is being prepared in the existing perception prompt; no record-count cap, schema change, formal-memory rewriting or model-parameter change is authorized by this finding. Addendum: `reports/onedir-third-window-fragmentation-addendum-r10.md`.

91. Source S001/S002 completed in genuinely new context `7d391325ee8a4752b2074d2c944247a9`, using actual memory searches to retrieve the project alias and unbooked travel status. The original main session remained byte-identical and was not loaded. The returned alias 青竹验收 matches frozen M013/S001; the controller takeover note's 北莲验收 was erroneous and was not used to correct the model or alter memory. Exit0 and process release are recorded in `reports/source-s001-s002-recall-r10.json`.

92. Source S011/S012 completed in `5d50b62a998a4b1f92f516bbe8fd2ca9`. S011 read the real image and returned its random code/source; its model-selected zoom scripts and Pillow installation mean this is not a tool-free vision-only measurement. After actual exit/restart and picker restoration, the same file hash, session ID and reference snapshot were retained. S012 identified the restored source and immutable snapshot. One reference was counted once; two ended turns produced zero automatic jobs. Both processes exited0, and all six observed launcher/driver/app PIDs were absent afterward. Report SHA256 `ca0f6dc0b977fe32b1a58b2c3080067b287080d4a307dd4948b7952945504c5b`. This two-turn state is now available for the separate real pip S013/S014 cross-runtime continuation without copying history.

93. Daily pip S019/S020 reused its S015/S016 session, adding exactly two frozen prompts after genuine load cancellation. Current-grid checks proved Ctrl+C closed the modal; cancellation preserved the original file hash, ID and two-turn count. The two subsequent replies were session-grounded, with four ended turns, one recovery file and no new memory afterward. Exit0, released owned PIDs and zero unexpected warnings/errors are independently recorded. A helper launch containing an accidental literal `--` failed before any app start and is classified as a driver error, not an application test failure. Evidence: `pip-short-urgent-r10-20260919/evidence/S019-S020-real-pip-load-cancel.json`.

94. Source S003/S004 used a separate project `9723f77cc05b77f2a1ac82a6` with the same isolated test-user memory namespace. Current-project searches returned no 青竹 episode; the explicit 白石 request then produced record `e22b6984a47ae8c68ffae93872263296`, bound only to the new project and backed by a1024-dimensional vector. Original source records/session hashes remained unchanged. Two turns, one explicit job, zero automatic jobs and exit0 are verified. The model nevertheless created a local full-scan helper while over-investigating S003; that unnecessary artifact is recorded as real behavior, not evaluator work.

95. Source S015/S016 has a split verdict. Runtime mechanics passed: the original plus five urgent texts retain six ordered boundaries in one outer turn, and SDK request message18 contains all five urgents together. The later B→D correction is preserved and S016 is a separate single-input turn. Business-quality acceptance failed: S015 used34 tools over215.257 seconds and investigated evaluator control/report/scenario materials. Its correct final todo text is therefore not counted as uncontaminated task performance. The `/stop` request arrived after completion and truthfully returned no active task; it did not cancel a running task. No extra correction prompt was sent. Both turns, exit0 and process release are preserved in `reports/source-s015-s016-urgent-r10.json`, SHA256 `5a8aa4ec4b207c51f3b7ab3d0a0d4c9ebed782d3e422a110626eee366e442440`. Exact configured-key scanning found no matching key in this session or the source image session; this is not a claim about unknown credentials or uncaptured traffic.

96. The narrow perception grouping prompt is now edited, and offline reconstruction confirms20 new turns plus3 overlap. Its proposed real diagnostic replay was rejected by automatic approval review before process launch because the specific frozen-session transmission and C-drive test namespace required explicit permission. User confirmation has been requested with both scopes stated. No replay API result exists, no replay C namespace was created, and original data remains unchanged. The prompt fix is UNVERIFIED; no bypass or alternate transmission was attempted.


97. EXE main M059–M063 completed as actual62–66. M064/actual67 failed at automatic compression (16384), and M065/actual68 failed identically. M065 was mistakenly advanced by the evaluator after treating the driver's `observed` counter as success; this is not an application retry. No M066 was sent. Successful compression count remains1, and three perception windows remain indexed. Normal `/exit` returned0 and both application/driver PIDs were released. Main-session SHA256 `bda552c538d5b8445c155179be65d0b6f35f8caced2d03ca39f0e7ece12aa73b`; failure report `runtime/onedir-r10-m064-m065-compressor16384-stop.json`, SHA256 `2fe937012ea2222b102905c8608a8c507f025ee71e63c642ac183badc0bca528`. Partial frontend artifacts are not a passed delivery; independent CORS preflight returned405. All three main conversations are now blocked by compression failure, with parameters unchanged.

98. Current-source focused auxiliary regression passed16 tests and skipped1 because the project venv lacks the build frontend. The skipped PEP517 test was then actually run in the user's existing Miniconda environment: it failed after37.37 seconds when wheel construction from the sdist could not create a deeply nested Skill resource. Failure log `reports/aux-r11-pep517-daily.log` is retained; path-length diagnosis is open. A skipped test is not counted as passed, and the prior successful r10 build does not erase this failure.


99. Terminal observation now records compact committed turn identities/statuses and resolves the exact requested turn. The final bounded harness suite passed49 tests in17.41 seconds; read-only EXE evidence independently confirms turn66 success and turns67/68 failed even though storage error is null. No application/config/session file was changed. Existing controllers do not hot reload. A counter or receipt is still not semantic acceptance, and initial new-session selection must be matched to the actual input/session identity.

100. Reverse pip-to-source S013/S014 completed as actual5/6 in shared session `5d50b62a998a4b1f92f516bbe8fd2ca9`. One file/revision90, preserved earlier transactions, one unchanged image snapshot and unchanged original prompt snapshot prove continuation rather than history copying. Fresh real ConPTY grids show the full long project path, full session ID and input box after `#session-context` height:auto. Final cumulative usage was35 responses,1,727,143 input/82,893 output;95.8179% cache hit with full coverage. This is the mixed-runtime short-session aggregate, not either environment's final main+worker cache gate. Normal exit0 and process release verified. Report `reports/source-s013-s014-cross-runtime-r11.json`, SHA256 `845180DA708F0C6032B316AE1F1C34F34BB3A235D9DAFCAC2ECF4414C16B8190`. S014 remained tool-heavy at roughly117 seconds.

101. The first r11 source urgent retest did not exercise the required timing. The evaluator waited on a nonexistent observer.active_turn field, then observed the actual running journal but used a slow patch to create five action files sequentially. The first urgent arrived4.105 seconds after S015 ended and correctly became a new outer turn. This is an evaluator-control failure, not an application urgent bug. The same attempt also retained unnecessary directory investigation (21 tools/124.442 seconds), so the prompt-quality fix is not accepted as complete. Its original report and hashes remain preserved; the next timing attempt pre-stages separate action files and publishes them together while directly verifying live active_turn.


102. The PEP517 failure is now localized to evaluator staging: a101-character inherited TEMP expanded to a266-character wheel destination while Windows LongPathsEnabled=0. The unchanged package passed with a short E: TEMP before the test repair. The test now supplies a211-character worst-case staging path under project WorkDatabase/runtime; the original external-long-TEMP invocation passed, followed by all11 packaging tests and a fresh focused repeat. No production resource, dependency, registry setting or build configuration was changed. Report `reports/pep517-long-path-r11.json` retains the original failure and host-policy limitation.

103. Source r12 urgent replay correctly delivered all five U02 texts during the same live turn, and one SDK request contains all five. The original plus five messages retain six original boundaries, then S016 forms only the second ended turn. Both final answers use D over B and preserve A/C/E. Business efficiency still failed: S015 used15 tools/202.361 seconds and read its own logs/session; S01635.477 seconds included an unnecessary log reread. Normal exit0/all three owned PIDs released; original formal config hashes unchanged. Report SHA256 `8f2bf03e92dea9a0bb5e748b9c533ec1b6228b827475f8b69441852c652d6261`. The operator initially looked for running status among ended-turn records and corrected that poll from active_turn before dispatch; this delayed, but did not duplicate or misassign, the five inputs.

104. A prompt-order audit confirmed common_conduct's unconditional memory lookup rule preceded the coordinator's conditional lookup rule. Only that condition and the missing-material clarification order were aligned. Source r13 pre-staged U02 and dispatched within0.098 seconds of the live turn creation, with five separate real keyboard submissions. Its first actual model response received all five user updates yet still chose to inspect runtime records, despite quoting the new instruction in its reasoning. This is a remaining model/task-behavior failure, not a missing urgent message or old prompt snapshot. Further prompt accumulation or an unapproved tool policy is not substituted for a demonstrated fix.


105. Source r13 closed normally with two successful ended turns and zero automatic jobs; owned launcher/driver/app PIDs were released. Real urgent mechanics passed even when all five supplements were already in the first request. S015 still took158.106 seconds/18 tools; S016 took24.510 seconds/3 tools. The prompt's missing-material instruction was present and explicitly recognized in the model reasoning, yet self-session investigation continued. Report `reports/source-s015-s016-urgent-r13.json`, SHA256 `85da610cdaee275813b0507a37a361579f36345e7ae63cbf76af0a374221fb84`. Auxiliary prompt snapshot/layout/loading checks separately passed14 tests in28.37 seconds. Formal source/global configuration hashes still equal their recorded baselines.

106. New onedir S011/S012 attempts ended before any real model response with output-budget/no-input-capacity validation; they are not vision acceptance. Startup logs show metadata networking denied with WinError10013 when the evaluator launched inside the execution sandbox, causing393216 context fallback to equal393216 output budget. A later pip S009 attempt also failed at that boundary and S010 was not sent. Exact launch-permission and effective-configuration diagnosis is in progress; real daily-permission retries must preserve the original failed/blocked attempts and must not change model values to force a pass. This ordinary new-user test is distinct from the explicitly rejected frozen-memory replay, which remains unexecuted pending authorization.

107. Daily pip r11 completed frozen S001–S004 and S007–S010 in four genuine new two-turn sessions. Independent Lance inspection found seven matching memory/mirror rows, project-only 白石 and failed-result records, no untrusted instruction records, and unchanged MEMORY.md. All accepted apps exited0 and all24 recorded owned PIDs were absent. The original S009 sandbox pre-request failure remains separate; the unchanged-config daily-permission retry executed the fixture with exit7. Report `reports/pip-s001-s010-memory-r11.json`, SHA256 `8B2E7F30D462200B782B450BAA12C003848B966A1A44087C62C9734A012EF3D1`. S005/S006 remain unexecuted because their actual forgetting prerequisite is not satisfied.

108. Onedir r10 completed S011–S014 under real application permissions. S011/S012 retained earlier sandbox-failed turns1/2 and passed as actual3/4, with a real exit/load between them. S013/S014 loaded the original pip image session3132c227894d486cba0d3288d0c1cf3f directly and appended actual3/4 to the same file; reference hashes and load-time file hash remained unchanged. Three accepted app runs exited0 with PIDs released. Four automatically archived tool results are disclosed, not mislabelled as no writes. Report `onedir-short-image-r11/reports/onedir-s011-s014-r11.json`, SHA256 `d9f809c93ff14d32d631d8db8852a94bbb19cb29c2f39823bf487a0b7bfa326b`. These results use the r10 artifact, not the current source containing subsequent fixes.

109. Current main-session resource/cache audit r13 preserves all unique final usage identities, including cold and failed turns. Known main+ordinary cache ratios are98.835%/99.062%/98.618%; worker cache/reasoning detail is missing for3/3/5 identities, so coverage and final acceptance remain separate. No explicit compressor/title usage identity is present: absence is not zero cost. Eight available20/40/60 boundary receipts were independently hash-checked; pip60 is absent. Native OS threads are not Agent threads, endpoint RSS is not peak memory, and changed PIDs/versions cannot establish a leak trend. Report `reports/main-resource-cache-status-r13.json`, SHA256 `36C63E56916D10E1B27BD166046190758770247EC279B2724491436EA0922AA2`.

110. A narrow configuration fix removes file-lock creation from `settings()` reads. It parses one bytes snapshot into a local cache tuple and returns a deepcopy; configuration writers retain ordered locks and atomic replacement. The old installed implementation reproduced a30-second C-drive sidecar-lock timeout under restricted write permissions; the same daily Python loading corrected source completed readonly settings in0.59 seconds, with no API call or configuration write. Three focused behavior tests first showed two expected failures, then passed; the related configuration/startup/entry group passed110 tests in238.16 seconds. This corrects readonly access, not unavailable network metadata or compressor exhaustion, and has not yet been installed into a new final artifact.

111. Source idle CLI lifecycle r14 passed six real terminal checks: empty startup, `/load` then Ctrl+C, successful project switch, rejected nonexistent-project switch retaining the previous project and editable input, empty `/clear`, and normal `/exit`. Both test projects retained zero session files throughout; the observer stayed unbound. Owned PIDs3144/30292/27880 were absent after exit0 and the selected configuration hash stayed unchanged. This is idle lifecycle coverage, not proof of active-task cancellation. Report `reports/source-cli-lifecycle-r14.json`, SHA256 `170D43CC8D50EBADE60F109AD3904DC29073B51245B4F82227CA2E2283F6DC14`.

112. Candidate r14 wheel/sdist built through the daily Miniconda interpreter in25.94 seconds, exit0; eight package-content/source/sanitization checks passed. Wheel SHA256 `dc9eaf1329d1aafa5cea239c68e4ae7b3531ae08c4b8e98e45868c539687eb81`, sdist `9e18631839eaa97f7db9d9610bfba807d33e42a0a9bffd2a10c8ff83014b32c8`. The wheel replaced only RedLotus in the existing daily installation using no-index/no-deps. A new restricted-permission process importing the installed site-packages config module read the C-drive global JSON successfully in0.015 seconds; module SHA256 `faaa6d253632ad3b060b391648679d4c34c1612a504c47a6c0d6c8e887f0eb91`. The call printed metadata only and performed no model/network request or configuration write. Installation evidence is under `build-wheel-r14`; the package remains1.0.1 and is candidate-artifact replacement, not a public version upgrade.

113. Read-only r14 usage addendum corrects the r13 report's zero-placeholder statement: there are3 source,3 pip and5 EXE worker rows with all usage counters zero and no model/provider identity. SDK deferred-capability bookkeeping is the strongly supported explanation, but the old rows lack parts/tool-call IDs, so individual historical provenance is not proved. The original r13 report remains unchanged; no rows were deleted and no final cache gate is declared passed. The addendum independently replayed the final journal maps and matched all11 identities. Report `reports/main-usage-zero-rows-r14-addendum.json`, SHA256 `21AB32E15AC2DC18329DF0AFE23C4AF4E55D3E204E7266E5585550B771C91DCF`.

114. The newly installed daily r14 entry completed real S019/S020 regression as actual5/6 in existing short session74747e3db31d4c34894f82b4058f9610. Startup restored the same file; opening `/load` then Ctrl+C kept its hash, usage and file count unchanged. Each frozen prompt was sent once, both ended successfully, and no automatic memory job or error log appeared. Exit0 and all four recorded owned PIDs absent. This repeats existing cases to verify the installed candidate and does not add new case coverage. Evidence: `reports/pip-installed-r14-regression.json` and `.md`.

115. Onedir r14 built using daily Miniconda in472.44 seconds with exit0. Its100 source fingerprints match both the r14 wheel and the current application source. Build and post-extraction resource audits each passed8 checks; the extracted payload contains3332 files and EXE SHA256 `954beb4e491a7143cd6e593c3698763a821577cadf7851f5f3f98898e015ca86`. The first post-extraction audit was mistakenly invoked while the staging process was still running and reported no payload files; that premature evaluator result remains `audit-postextract-r14.json`. After extraction exited0, the actual ready check passed in `audit-postextract-ready-r14.json`. This does not yet prove EXE runtime acceptance; no failed artifact was published.

116. Onedir short-memory r14 completed frozen S001/S002 and S007/S008 through the actual r10 EXE. New contexts correctly recalled the project alias and unbooked travel state, and rejected the referenced injection without memory writes. S003/S004 and S009/S010 were not started before that bounded batch ended. Both apps exited0 with owned PIDs absent; original main bytes/hash and configuration hash stayed unchanged. The initial S001 receipt briefly selected the old main's historical turn1; acceptance instead used the actual new session ID, ended-turn identity and current VT answer before S002 was sent. Evidence `reports/onedir-s001-s010-memory-r14.json`, SHA256 `a81600c9af940ca3f965e55c1eb4a09438c0cc20e419ecf0d0371cd384f2e8aa`.

117. The terminal mailbox now captures completed-turn identities before `send + until_turn`, so an old session's matching turn number cannot be accepted as the new response. Unreadable baselines return explicit unknown/attention status; snapshot-only historical queries retain their prior behavior. Two new tests first failed then passed; all51 terminal observer/controller tests passed in14.50 seconds. Only fresh drivers use this repair. It does not prove ownership of an unrelated turn already active before submission; session ID and real response evidence remain necessary.

118. Extracted onedir r14 passed real startup restoration of session6d8378445bc84e9fa2bc10186297e03d at count2, `/load`/Ctrl+C cancellation with unchanged bytes/hash/usage, then frozen S019/S020 as actual3/4. Both model responses succeeded. The file grew in place to114562 bytes; no automatic job appeared. Original main and configuration hashes remained unchanged; no WARNING/ERROR/CRITICAL/Traceback occurred in the application log. Exit0, driver32536 and EXE6136 absent. Report `reports/onedir-installed-r14-regression.json`, SHA256 `E0A55A093E56CD78A8C9CB58827902FFDFC425CA0227F09DB69EBA3DEAC17396`. This is installed-candidate regression, not additional frozen-case coverage or full EXE acceptance.

119. Current full auxiliary suite r14, run in the actual project `.venv`, finished in608.11 seconds with664 passed,2 skipped and2 failed; process exit1 is preserved. Both failures are the previously accepted layout exceptions: core contains10 Python files and seven files exceed500 effective lines. The full log SHA256 is `6C4F59EB037FF6CFF417E6671EC6724205CCFD266099BBCB778AA2C596834B02`. A separate `-rs` check confirmed the skips: the project Anthropic SDK uses standard httpx, and the project interpreter lacks the optional build frontend. Daily Miniconda's httpx2 transport check passed1 test in5.79 seconds; real r14 wheel/sdist construction already exercised its build frontend. Neither auxiliary result is a real Anthropic service request. Default-sandbox refusal to start the uv-backed project interpreter during skip diagnosis is separate evaluator-permission evidence, not an application test failure. No model/RAG/threshold parameter was changed.

120. User explicitly authorized test compressor output131072 while keeping thinking enabled/max; the earlier disable-thinking suggestion is superseded. Exactly `models.compressor.max_tokens` changed in the three existing isolated profiles, from16384 to131072, with backups and structural equality checks for every other field. Formal source/global configuration bytes remain unchanged. The compressor prompt now explicitly says to compress historical context only, retaining its eight-section checkpoint contract. Source effective target confirms DeepSeek/openai-chat with131072/max/enabled. Compression/gateway auxiliary tests passed15 in19.25s; existing provider-body reasoning tests separately passed5 in8.47s without real requests. The first auxiliary launch was refused by the uv trampoline in the default sandbox; the permitted actual project interpreter ran the tests. The first terminal evaluator incorrectly used Miniconda without winpty and never started RedLotus; the existing project evaluator then restored the original source session. M063 is replayed once as actual67 using its exact original prompt, with automatic compression required. Evidence: `reports/compressor-128k-config-r15.json`, `reports/compressor-r15-effective-source.json`, `reports/source-m063-r15-before.json`. No successful real compression is claimed yet; frozen perception replay remains unexecuted pending its distinct approval.

121. Source original M063 completed successfully as actual67 after one real automatic compression (27026 input,14206 output,9204 reasoning;143→20 messages). The real Worker wrote the backend and the parent reviewed it. An independent evaluator used loopback HTTP for16 authentication/isolation checks, all passed, and verified the backend Python hashes still matched after parent review. The evaluator did not supply implementation code or edit the target backend. Compression fidelity nevertheless failed: one new checkpoint phrase changed the correct hotel570 to580; the prior journal/summary had570. Other selected goal/control constraints survived. During the same turn, a genuine TUI issue hid the multiline recursive-deletion command/options inside a single-line input placeholder. The evaluator declined with `n`; the reply stayed in turn67. Observed total duration includes this confirmation wait and is not API-only time. Normal exit0, all owned PIDs released, active turn null, perception consumed through60; M064 was not sent. Reports: `source-m063-compressor-r15.*`, `source-compressor-r15-fidelity.*`, `source-m063-r15-api.json`.

122. Daily installed pip r15 replayed the exact original M043 once as actual47 in sessionedcf8e9585e0447da7b84bf3119fc096. Automatic compression succeeded in one request:28332 input/640 cache-read/15013 output/11852 reasoning,167→10 messages, committed checkpoint message402/sequence119. The task completed successfully; independent syntax/handler and screenshot checks passed. No confirmation question occurred. M044 was not sent, profile stayed at the authorized128K/max configuration, exit0 and all owned processes released. Evidence `reports/pip-m043-compressor-r15.json`, SHA256 `1066EFD57BC6C487F6E90AE1A23E0F409F41D0DED66DABECF88DA6AC75517925`. This closes that prior compressor-exhaustion reproduction, not the full100-turn or three-compression gate.

123. r15 candidate wheel/sdist and onedir were built sequentially using daily Miniconda, audited against frozen source and installed/extracted without dependency changes. Wheel SHA256 `c4de7909bc81e7941230974e51611811bb9db36898c5b27fce88c98a11d865fd`; sdist `e6773aa25cdc0f3f2c92dc9b6664f0628c10e3ae1904872a8a925d71f2bc0685`; onedir ZIP `e323f1a41a47fe0ef2f06c897586ac147418529a7d92b523f8ed29476df1fa4e`; extracted EXE `ee27704302a06544e72c4d43f09c61698c80deff96809a384a31b00d758a5f3a`. Build/content checks passed; neither those checks nor source/pip retests approve the summary-quality failure. No release/merge/push occurred. Evidence `reports/r15-wheel-install-and-onedir-build.*`.

## Storage and cleanup

Test projects, build/download caches and evidence belong to this run's E: directory. Only isolated native LanceDB test data may use the user's `.redlotus` evaluation namespace. Existing configuration, memory and sessions are protected.
