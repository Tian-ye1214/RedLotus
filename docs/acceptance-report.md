# 网关、全局数据与安装体验验收报告

日期：2026-09-13。实现分支：`codex/gateway-reference-experience`。

**用户已明确要求先合并到 develop，后续测试由用户进行。** 三轮六类功能验收、pip 与 PyInstaller 实际入口均已通过；持续测试按用户要求停止于 76 个回合、2,726.28 秒（约 45 分钟）。两小时/200 回合未完成，总体缓存命中率 89.20%，尚未达到大于 90% 的完整验收要求。本文不将这些未完成项标为通过。

## 配置与数据归属

安装入口从当前操作系统用户的全局配置目录读取 config.json，不使用启动目录中的配置或 .env。源码 main.py 显式使用开发配置。全局配置、核心记忆、LanceDB、会话、引用原件与感知任务跨项目和安装版本共享；项目情景按 project_id 隔离，WorkDatabase 继续保存项目产物。

安装测试的主 Agent、Manager、Worker 均为 deepseek-v4-flash，最大输出 393,216 token。记忆感知使用 Worker 配置，与压缩独立；compressor 最大输出为 16,384 token。开发配置保持原样，其余模型、RAG 参数及上下文阈值未为测试改变。

- 全局配置 SHA-256：`7c266e63f4ded1aff7d1a173c3140b5da1c2b7ad7e5f90fe701bba4b44487095`。
- 开发配置 SHA-256：`10ab1eb59183f5baf201161305a8ac5c367ea7ffa85142a171a3cdb365e3dd02`。

测试数据与个人记忆隔离，未预填最终记忆或向量。配置修改入口有文件锁和原子写入，业务读取使用深拷贝。初始化在运行入口完成，随包模板不带真实凭据。

## 三轮真实功能结果

最终测试候选版为 candidate-224925。下表每轮均完整执行六类场景；模型、embedding、rerank 均访问当前配置的真实服务。

| 轮次 | 六类场景 | 模型请求 | embedding | rerank | HTTP 200 | 非预期 WARNING/ERROR |
|---|---|---:|---:|---:|---:|---:|
| 1 | 全部通过 | 167 | 34 | 15 | 216 | 0 |
| 2 | 全部通过 | 175 | 57 | 19 | 251 | 0 |
| 3 | 全部通过 | 169 | 27 | 12 | 208 | 0 |

| 类别 | 实际验证内容 |
|---|---|
| 对话 | 多轮追问、FIFO 连续输入、工具期间 urgent、stop 后继续，实际消息及工具结果正确 |
| 通用 Agent | 两个真实子 Agent 核对 CSV，生成并运行脚本，独立核对 North=35、South=40；真实浏览器交互、截图与 Skill 执行 |
| 三层记忆 | 每轮至少 45 个真实用户回合，25/5 聚合窗口、主动保存、跨项目召回、项目隔离、纠正、遗忘、并发恢复与收尾；凭据不落入正式记忆 |
| 引用 | 随机图片、PDF、DOCX/DOC、CSV、XLSX/XLS、PPTX/PPT、Markdown、HTML，原件及文档内随机图片识别；20/21 文件、大小、重复路径及快照检查 |
| 长任务 | 沿用配置阈值真实触发压缩，压缩后仍能回答最初和中途约束，原始轨迹保留 |
| 交互 | CLI 控制器与 Textual 实际运行；diff 保留/撤销、目标模式、状态、配置查看/取消、clear、load、cd 等 |

辅助回归为 **97 项通过**。它们用于协议与边界定位，不替代上述真实验收。

## 安装程序与连续运行

| 实际入口/场景 | 结果 | 耗时 |
|---|---|---:|
| 干净环境 wheel 安装后的原生 CLI | 对话、图片、文档、子 Agent、产物、记忆、退出通过 | 554.25 秒 |
| PyInstaller onedir 原生 CLI | 同上通过 | 409.16 秒 |
| PyInstaller onefile 原生 TUI | 同上通过 | 500.27 秒 |
| pip → 冻结程序恢复、共享记忆、并发更新 | 实际召回工具返回两个并发保存值，旧会话继续可用 | 301.72 秒 |
| 冻结程序 diff、目标模式、clear/load、长文本压缩 | 8 个长文档回合后实际压缩，早期及中途约束保留；峰值实际输入 461,673 token | 628.88 秒 |
| 冻结程序旧 Office 与 Markdown/HTML | 外部 LibreOffice 转换、正文与内嵌图片识别通过 | 129.67 秒 |

两个初次扩展测试的失败已保留：一项错误要求保存回复必须重复标识文本，实际已返回有效回执；另一项错误要求单一历史记录也必须展示选择框，实际已经自动加载。测试修正为核验真实召回结果和实际恢复状态后重测通过，没有改动产品逻辑来迎合断言。

连续任务以维护销售核验脚本为主线，真实追加数据、修改门槛、运行脚本并独立核验每次产物，穿插长参考日志和早期约束追问。用户要求提前合并后停止，完成 76 回合、约 45 分钟，期间非预期 WARNING/ERROR 为 0。线程数、内存、句柄、子进程和记忆积压均有采样；不能据此声称已通过两小时稳定性与最终收尾验收。

## 缓存统计与仍未达标项

缓存比例为服务实际返回的缓存命中 token / 全部输入 token，包含冷请求、压缩与感知；按请求 ID 去重。原生终端扩展测试与诊断重放没有混入下面的矩阵/连续运行统计。

| 范围 | 命中 token | 未命中 token | 命中率 |
|---|---:|---:|---:|
| 连续 76 回合任务 | 54,115,328 | 2,834,668 | 95.02% |
| 三轮矩阵与连续运行合计 | 65,062,144 | 7,877,862 | **89.20%** |

三轮独立矩阵各自为 68.08%、69.15%、68.10%，其中包含大量首次输入的长文档与独立感知请求。总体大于 90% 的要求尚未满足，不将连续会话单独达标替代总体结果。

请求审计确认持续会话的主 Agent system 前缀固定；Skills 布局/摘要、系统信息、行为规则与核心记忆均仍存在。时间与运行反馈追加在新消息中，记忆写入不重写当前会话的系统前缀。下一步应在用户实际使用场景中继续评估冷输入、感知和压缩的占比。

## 本轮修复与分发检查

- 统一 SDK 模型工厂和请求边界，配置预设及角色覆盖保留用户值；修正 DeepSeek 实际输出长度字段、推理历史与推理级别映射。其他真实网关尚未验证。
- 请求预算同时计入系统/工具开销和配置输出容量，修复输入加输出超过服务总容量的问题，没有降低配置阈值。
- 修复 SDK 与用量解析依赖版本不兼容导致的零用量，HTTP 用量另行独立审计。
- 感知曾输出 XML 工具标签导致校验失败，现使用 SDK JSON 结构化输出；原失败输入三次真实重放均单请求成功。没有增加重试次数。
- 压缩曾用完 16,384 输出预算且缺少章节，现明确完整八节恢复检查点并归纳重复日志；原失败上下文真实重放通过，配置未改。
- 修复摘要来源在 SDK 合并消息后丢失、项目切换阻塞于旧感知、配置/状态读写竞争及恢复覆盖较新结果等问题。
- 引用在接收时固定原件，后台解析；完善 CSV、HTML、分组 PPT 图片及队列引用处理，保留 RAG 生产与索引分离。
- wheel、sdist、onedir、onefile 的文件清单与实际凭据值扫描通过，没有开发配置、个人记忆或日志进入分发包。

实现使用 [Pydantic AI 原生模型与输出接口](https://pydantic.dev/docs/ai/core-concepts/output/)；服务兼容依据真实失败及 [DeepSeek 协议说明](https://api-docs.deepseek.com/guides/thinking_mode/)。安装与复现步骤见 [安装说明](gateway-installation.md)。

## 清理与未验证范围

仓库 build 已删除。dist 删除遇到系统“目录不是空的”错误，随后删除重试被自动审批拒绝（仅返回 blocked by policy，未提供详细原因）；dist 残留及本机验收目录内的临时冻结产物尚未清理完。原始证据、配置与源代码保留。

视频按用户要求暂缓。OpenAI Responses、Anthropic、Google 与跨服务切换只有辅助协议测试，没有对应真实服务验收。QQ/微信本人渠道未实际测试。用户将继续执行剩余长时间及缓存验收。

本机证据目录为 `%LOCALAPPDATA%/RedLotus/acceptance/gateway-20260913-155450/candidate-224925`，保存报告、逐次实际用量、原始轨迹、真实产物、终端证据和失败记录。后续合并由用户最新明确指令授权，不代表未完成的验收条件已满足。
